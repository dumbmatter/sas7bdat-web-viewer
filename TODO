Don't read whole file if it's huge
  incremental reading possible, like in node?
  if not possible, maybe don't render whole file if it's huge

sticky table header
  https://facebook.github.io/fixed-data-table/ - no, can't auto figure out column sizes
  https://github.com/jmosbech/StickyTableHeaders
  https://github.com/kingkool68/stickyHeader
  https://css-tricks.com/persistent-headers/
  others, didn't look much

show header info

parsing in web worker, with progress bar

react prod mode

error handling
  display errors/warnings
  show filename as danger
  csv errors

responsive
